--- src.org/PartFile.cpp
+++ src/PartFile.cpp
@@ -298,6 +298,9 @@ void CPartFile::CreatePartFile()
 		//#warning Code for full file alloc - should be done on thread.
 	}
 	
+	// create whole file as sparse
+	m_hpartfile.Seek(GetFileSize() - 1, wxFromStart);
+	m_hpartfile.WriteUInt8(0);
 	
 	m_hashsetneeded = GetED2KPartHashCount();
 	
